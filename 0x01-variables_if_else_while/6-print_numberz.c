#include <stdio.h>
#include <ctype.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

int dafe;
for (dafe = 48; dafe < 58; dafe++)
putchar(dafe);
putchar('\n');
return (0);
}
