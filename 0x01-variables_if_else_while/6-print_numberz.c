#include <stdio.h>
#include <ctype.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

int dafe;
for (dafe = 0; dafe < 10; dafe++)
putchar(dafe);
putchar('\n');
return (0);
}
