#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

char alpha[] = "0123456789";
int dafe;
for (dafe = 0; dafe < 10; dafe++)
putchar(alpha[dafe]);
putchar('\n');
return (0);
}
